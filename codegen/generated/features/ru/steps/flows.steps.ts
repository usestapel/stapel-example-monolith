// Generated by generate_flow_features (language: ru) — do not edit.
// flow-system.md §3: the flow is the source, these step-defs are a projection.
// HTTP steps drive the codegen typed client (@stapel/core StapelClient); human/UI
// steps are TODO(testid) stubs (no testid plan on the flow yet, system-design §7.20);
// action/function/task steps assert a backend side-effect and are pending.
import { createBdd } from "playwright-bdd";
import { test } from "./fixtures";

const { Given, When, Then } = createBdd(test);

// auth.password_login — step 0 (human)
Given(/^Пользователь вводит логин и пароль на форме входа$/, async ({ stapel }) => {
    // TODO(testid): UI step — attach a testid plan to flow auth.password_login step 0 (system-design §7.20).
    throw new Error("pending UI step: auth.password_login step 0");
});

// auth.password_login — step 1 (http · POST /auth/api/password/login/)
When(/^Проверить пароль; 423 при блокировке; при включённом TOTP и PASSWORD_LOGIN_STEP_UP — ответ TOTP_REQUIRED c challenge_token$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/password/login/", { method: "POST" });
});

// auth.password_login — step 2 (http · POST /auth/api/totp/challenge/verify/)
Then(/^Опциональный шаг \(только при TOTP_REQUIRED\): обменять challenge_token и код аутентификатора на JWT-сессию$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/totp/challenge/verify/", { method: "POST" });
});

// auth.passwordless_login — step 0 (human)
Given(/^Пользователь вводит email на форме входа$/, async ({ stapel }) => {
    // TODO(testid): UI step — attach a testid plan to flow auth.passwordless_login step 0 (system-design §7.20).
    throw new Error("pending UI step: auth.passwordless_login step 0");
});

// auth.passwordless_login — step 1 (http · POST /auth/api/email/request/)
When(/^Запросить одноразовый код на email; 429 при рейт-лимите, 422 при блокировке$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/email/request/", { method: "POST" });
});

// auth.passwordless_login — step 2 (http · POST /auth/api/email/verify/)
When(/^Обменять код на JWT-сессию; неверный код уменьшает счётчик попыток$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/email/verify/", { method: "POST" });
});

// auth.passwordless_login — step 3 (action · user.registered)
Then(/^Эмитится при первом входе — профиль и воркспейс создаются подписчиками$/, async ({ stapel }) => {
    // TODO: assert the action side-effect `user.registered` of flow auth.passwordless_login.
    throw new Error("pending effect assertion: auth.passwordless_login user.registered");
});

// auth.step_up_verification — step 0 (human)
Given(/^Клиент вызывает защищённый эндпоинт и получает 403 с конвертом verification: challenge_id, scope, factors, expires_at$/, async ({ stapel }) => {
    // TODO(testid): UI step — attach a testid plan to flow auth.step_up_verification step 0 (system-design §7.20).
    throw new Error("pending UI step: auth.step_up_verification step 0");
});

// auth.step_up_verification — step 1 (http · GET /auth/api/verification/<str:challenge_id>/)
When(/^Прочитать challenge: scope и факторы, отфильтрованные до реально доступных пользователю; 404 для чужого\/истёкшего challenge$/, async ({ stapel }) => {
    // TODO: fill the path parameter(s) of GET /auth/api/verification/{challenge_id}/ from a prior step.
    throw new Error("pending parametrized request: auth.step_up_verification step 1");
});

// auth.step_up_verification — step 2 (http · POST /auth/api/verification/<str:challenge_id>/initiate/)
When(/^Инициировать выбранный фактор: отправить код \(otp_email\/otp_phone\) или получить WebAuthn-опции \(passkey\); totp инициации не требует$/, async ({ stapel }) => {
    // TODO: fill the path parameter(s) of POST /auth/api/verification/{challenge_id}/initiate/ from a prior step.
    throw new Error("pending parametrized request: auth.step_up_verification step 2");
});

// auth.step_up_verification — step 3 (http · POST /auth/api/verification/<str:challenge_id>/complete/)
When(/^Завершить challenge доказательством фактора; успех = \{verified, verification_token\} \+ grant сервер-сайд; 400 при неверном коде, 423 когда challenge сгорел от перебора$/, async ({ stapel }) => {
    // TODO: fill the path parameter(s) of POST /auth/api/verification/{challenge_id}/complete/ from a prior step.
    throw new Error("pending parametrized request: auth.step_up_verification step 3");
});

// auth.step_up_verification — step 4 (human)
When(/^Повторить исходный запрос — grant уже на сервере; stateless-клиент передаёт X-Verification-Token из ответа завершения$/, async ({ stapel }) => {
    // TODO(testid): UI step — attach a testid plan to flow auth.step_up_verification step 4 (system-design §7.20).
    throw new Error("pending UI step: auth.step_up_verification step 4");
});

// auth.step_up_verification — step 5 (http · GET /auth/api/verification/preferences/)
When(/^Опционально: посмотреть свои step-up-настройки — по строке \{scope, enabled\} на каждый scope, который пользователь трогал \(enabled=false выключает default_on-scope, enabled=true включает opt_in-scope; strict-эндпоинты настройки игнорируют\)$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/verification/preferences/", { method: "GET" });
});

// auth.step_up_verification — step 6 (http · PUT /auth/api/verification/preferences/)
Then(/^Опционально: изменить настройку \{scope, enabled\}\. ИНВАРИАНТ: выключение \(enabled=false\) само защищено @requires_verification\(scope=verification\.settings, level=default_on\) — без свежего grant'а придёт 403 с конвертом verification; включение step-up-подтверждения не требует\. Обе записи сбрасывают кэш политики в core$/, async ({ stapel }) => {
    stapel.response = await stapel.client.request("/auth/api/verification/preferences/", { method: "PUT" });
});
