# Runtime dependencies of the all-modules monolith.
#
# Stapel framework — PyPI ranges (the way any user installs it). This service
# hosts every feature module in one INSTALLED_APPS; pip resolves inter-package
# deps from these.
stapel-core>=0.36,<0.37
stapel-auth>=0.25,<0.26
stapel-gdpr>=0.5,<0.6
stapel-profiles>=0.14,<0.15
stapel-notifications>=0.16,<0.17
stapel-workspaces>=0.29,<0.30
stapel-billing>=0.11,<0.12
stapel-cdn>=0.14,<0.15
stapel-translate>=0.6.1,<0.7
stapel-categories>=0.6,<0.7

# Service runtime + framework deps (pulled in transitively too; pinned here
# for the image build).
Django>=5.1
djangorestframework>=3.14
djangorestframework-dataclasses>=1.2
drf-spectacular>=0.27
django-cors-headers>=4.3
django-redis>=5.4
PyJWT>=2.8
cryptography>=41.0
requests>=2.31
psycopg[binary]>=3.1
gunicorn>=21.2
celery>=5.3

# Dev / test (used by config.settings.dev and pytest)
django-debug-toolbar>=4.2
debugpy>=1.8
pytest>=7.4
pytest-django>=4.7

# Codegen tooling (used by codegen/generate.sh -> `python -m stapel_tools.codegen`,
# invoked by `make codegen` / `make codegen-check`). Same PyPI-range install as
# everything above — CI's codegen-check job installs only from this file, so if
# it's missing here the generator can't run there even though it runs fine
# against a local dev checkout that happens to have it installed already.
stapel-tools>=0.50,<0.51

# Add service-specific dependencies below.
