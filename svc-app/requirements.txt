# Stapel framework — PyPI ranges (the way any user installs it).
# Pre-1.0 semver: minor = breaking, so ranges cap at the next minor.
stapel-core>=0.3.1,<0.4
stapel-auth>=0.3.2,<0.4
stapel-gdpr>=0.3.1,<0.4
stapel-profiles>=0.3.1,<0.4
stapel-notifications>=0.3.1,<0.4
stapel-workspaces>=0.3.1,<0.4
stapel-billing>=0.4.0,<0.5
stapel-cdn>=0.4.0,<0.5
stapel-translate>=0.4.0,<0.5
stapel-categories>=0.4.0,<0.5

# Service runtime
psycopg[binary]>=3.1
gunicorn>=21.2
celery>=5.3

# Dev / test
django-debug-toolbar>=4.2
debugpy>=1.8
pytest>=7.4
pytest-django>=4.7
